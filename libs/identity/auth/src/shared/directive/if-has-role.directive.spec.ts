import { IfHasRoleDirective } from './if-has-role.directive';

describe('IfHasRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new IfHasRoleDirective();
    expect(directive).toBeTruthy();
  });
});
