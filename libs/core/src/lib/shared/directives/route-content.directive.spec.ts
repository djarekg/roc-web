import { RouteContentDirective } from './route-content.directive';

describe('RouteContentDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteContentDirective();
    expect(directive).toBeTruthy();
  });
});
