import { RouteContentHeaderDirective } from './route-content-header.directive';

describe('RouteContentHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteContentHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
