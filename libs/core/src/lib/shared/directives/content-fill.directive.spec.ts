import { ContentFillDirective } from './content-fill.directive';

describe('ContentFillDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentFillDirective();
    expect(directive).toBeTruthy();
  });
});
