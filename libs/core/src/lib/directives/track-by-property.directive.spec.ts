import { TrackByPropertyDirective } from './track-by-property.directive';

describe('TrackByPropertyDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackByPropertyDirective();
    expect(directive).toBeTruthy();
  });
});
