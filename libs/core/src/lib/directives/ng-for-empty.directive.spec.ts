import { NgForEmptyDirective } from './ng-for-empty.directive';

describe('NgForEmptyDirective', () => {
  it('should create an instance', () => {
    const directive = new NgForEmptyDirective();
    expect(directive).toBeTruthy();
  });
});
