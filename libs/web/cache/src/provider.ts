import {
  HTTP_CACHE_INTERCEPTOR,
  HTTP_CACHE_PREFLIGHT_RESPONSE_INTERCEPTOR,
} from './interceptors';

export const HTTP_CACHE_INTERCEPTORS = [
  HTTP_CACHE_PREFLIGHT_RESPONSE_INTERCEPTOR,
  HTTP_CACHE_INTERCEPTOR,
] as const;
